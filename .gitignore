# Test extraction artifacts (regenerated by *-test.mjs on every run)
*-extracted.cjs

# Regenerated build intermediate (make-atlas.mjs builds it from ai-network-template.html; atlas-build.mjs consumes it — run make-atlas before atlas-build on a fresh clone)
atlas-template.html

# Local Claude Code config (machine-specific paths, local permissions)
.claude/

# Unrelated stray file from another project
jeelpixel-file-network.html
archive/jeelpixel-file-network-standalone.html

# Regenerable raw MNIST archive for the 3D machine-learning lab
experiments/ml-3d-classifier/data/raw/
