# Test extraction artifacts (regenerated by *-test.mjs on every run)
*-extracted.cjs

# Regenerated build intermediate (scripts/make-atlas.mjs builds it from src/templates/ai-network-template.html; scripts/atlas-build.mjs consumes it)
src/templates/atlas-template.html

# Local assistant configuration (machine-specific paths and permissions)
.claude/

# Local development-server logs
.dev-server-*.log

# Regenerable raw MNIST archives used only while rebuilding trained lab data
experiments/ml-3d-classifier/data/raw/
experiments/nn-3d-simulation/data/raw/
